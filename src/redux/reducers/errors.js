import {} from "../constants";

const initialState = {};

const errors = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default errors;
